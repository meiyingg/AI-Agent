# 核心 LLM / Agent 栈（建议与黑马课程项目版本一致；create_agent + agents.middleware 需 langchain 1.x）
langchain>=1.0.0
langchain-classic>=1.0.0          # 1.x 后 EnsembleRetriever/ContextualCompressionRetriever 在这里
langchain-core>=0.3.0
langchain-community>=0.3.0
langchain-text-splitters>=0.3.0
langchain-chroma>=0.2.0
langgraph>=0.2.0

# 向量库 / 模型 (Tongyi / DashScope; DashScopeRerank 也走 dashscope)
chromadb>=0.5.0
dashscope>=1.20.0

# 高级 RAG: 关键词检索 (BM25)
rank_bm25>=0.2.2

# 在线检索 (功能二): LangChain 官方 Tavily 工具
langchain-tavily>=0.1.0

# 配置 / 环境
pyyaml>=6.0
python-dotenv>=1.0.0

# Web 后端 (FastAPI + SSE 流式)；前端 Next.js 在 web/ 下用 pnpm 管理
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
# 短期记忆: LangGraph checkpointer (SQLite)
langgraph-checkpoint-sqlite>=2.0.0

# 知识库上传: 文档抽取 (pdf/docx/xlsx)。文件上传 multipart 需 python-multipart。
# 音视频转写: DashScope ASR(dashscope, 已含) + 系统级 ffmpeg(需另装, 不在 pip)
pypdf>=4.0.0
python-docx>=1.1.0
openpyxl>=3.1.0          # 读取 .xlsx 表格(公司合同/台账)
python-multipart>=0.0.9

# 永久存储 (可选, 设了 DATABASE_URL / R2_* 才启用; 不设则回退本地文件)
# Neon Postgres: 对话历史(PostgresSaver) / 向量(pgvector) / kv 数据
psycopg[binary]>=3.1
psycopg-pool>=3.2
sqlalchemy>=2.0
langgraph-checkpoint-postgres>=2.0
langchain-postgres>=0.0.12
# Cloudflare R2 (S3 兼容): 原始文件
boto3>=1.34

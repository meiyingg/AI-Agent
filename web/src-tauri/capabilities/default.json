{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "默认能力集：核心 API + 用系统浏览器打开外链。",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "opener:default"
  ]
}
